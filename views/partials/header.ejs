<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>YelpCamp</title>
    <link rel="stylesheet" href= "https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.11/semantic.css">
    <link href="https://cdn.quilljs.com/1.3.4/quill.snow.css" rel="stylesheet">
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script type="text/javascript" src="https://cdn.bootcss.com/typed.js/1.1.4/typed.min.js"></script>
    <script type="text/javascript" src="/js/test.js">

    </script>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>

    <nav class="nav-bar">
      <div class="ui inverted menu">
        <a href="/" class="active item nav-item">
          YelpCamp
        </a>
        <div class="right menu">
        </div>
          <a href="/upload" class="item nav-item">Upload</a>
          <% if(!currentUser){ %>
            <a href="/login" class="item nav-item"></i>Login</a>
            <a href="/register" class="item nav-item">Sign Up</a>
          <% } else { %>
            <a href="" class="item nav-item"><i class="user circle outline icon"></i><%= currentUser.username %></a>
            <a href="/logout" class="item nav-item">Logout</a>
          <% } %>
        </div>
      </div>
    </nav>
    <div class="container">
      <% if(error && error.length > 0){ %>
        <div class="ui red message">
          <%= error %>
        </div>
      <% } %>

      <% if(success && success.length > 0){ %>
        <div class="ui green message">
          <%= success %>
        </div>
      <% } %>
    </div>
